#include<iostream>
#include<string>
using namespace std;
int main(){
    string S;
    cin >> S;

    if(username == "" || pwd == "" || profile == "" || role == "" || key == "") {
cout << "username: " << username << " pwd: " << pwd << " profile: " << profile << " role: " << role << " key: " << key << //n; 
     } 
    return 0;
}
